{
  "name": "pyrio",
  "version": "2.0.0",
  "description": "AI-Powered Task & Schedule Coordination",
  "main": "src/main.js",
  "scripts": {
    "build": "esbuild src/main.js --bundle --outfile=dist/bundle.js --format=esm",
    "watch": "esbuild src/main.js --bundle --outfile=dist/bundle.js --format=esm --watch",
    "serve": "esbuild src/main.js --bundle --outfile=dist/bundle.js --format=esm --servedir=."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
